{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "vite",
  "installCommand": "npm install && npm install --prefix frontend",
  "buildCommand": "npm run compile && npm run build --prefix frontend",
  "outputDirectory": "frontend/dist"
}
